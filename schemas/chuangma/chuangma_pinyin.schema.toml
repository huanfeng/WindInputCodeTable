[schema]
id = "chuangma_pinyin"
name = "创码拼音"
icon_label = "创"
version = "1.0"
author = "群友"
description = "创码+拼音混合输入方案，创码优先，拼音补充"

[engine]
type = "mixed"

# 仅引擎固定参数（方案构成 + 内部权重基线）；融合策略走全局 [schema.mix]。
[engine.mixed]
primary_schema = "chuangma"
secondary_schema = "pinyin"
codetable_weight_boost = 10000000
