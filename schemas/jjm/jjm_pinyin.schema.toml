[schema]
id = "jjm_pinyin"
name = "晶晶码拼音"
icon_label = "晶"
version = "1.0"
author = "群友"
description = "晶晶码+拼音混合输入方案，晶晶码优先，拼音补充"

[engine]
type = "mixed"

# 仅引擎固定参数（方案构成 + 内部权重基线）；融合策略走全局 [schema.mix]。
[engine.mixed]
primary_schema = "jjm"
secondary_schema = "pinyin"
codetable_weight_boost = 10000000
